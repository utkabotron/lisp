# AutoCAD LISP Scripts Collection

Коллекция LISP-скриптов для автоматизации работы в AutoCAD.

## Структура проекта

### Главный загрузчик
- **`MainLoader.lsp`** - основной файл загрузки всех модулей при старте AutoCAD

---

## Категории файлов

### 🔑 HK_ - Hotkeys (Горячие клавиши)

**`HK_Hotkeys.lsp`** - Обертки стандартных команд AutoCAD для быстрого доступа

| Команда | Описание |
|---------|----------|
| `CT` | Создание окружности по 3 точкам |
| `PC` | Переключение на план вида сверху |
| `BRF` | Разрыв линии с указанием первой точки |
| `VC` | Вставка из буфера обмена в исходные координаты |
| `VCX` | Вставка из буфера + план сверху |
| `rv` | Разворот полилинии (REVERSE) |
| `cc` | Копирование объектов |
| `hi` | Скрыть/показать атрибуты (VBA макрос) |
| `MM` | VBA макрос MoveMultiplyBlocksAttributes |
| `RB` | VBA макрос rotateblocks |

---

### 📦 BL_ - Block Tools (Работа с блоками)

#### **`BL_Block_Tools.lsp`** - Инструменты для 3D манипуляций с блоками

| Команда | Описание |
|---------|----------|
| `RX90` | Поворот блоков на 90° вокруг оси X |
| `RY90` | Поворот блоков на 90° вокруг оси Y |
| `PB3` | Перемещение блоков на Z-координату из 3D полилинии |

#### **`BL_Delete_Duplicate_Blocks.lsp`** - Удаление дубликатов

| Команда | Описание |
|---------|----------|
| `DelDupBlocks` | Удаление блоков с одинаковыми координатами (tolerance: 0.001) |

#### **`BL_Select_Blocks_On_Line.lsp`** - Выбор блоков

| Команда | Описание |
|---------|----------|
| *(команды из файла)* | Выбор блоков, расположенных на линии |

#### **`BL_Insert_Block_To_Points.lsp`** - Вставка блоков (основной модуль)

| Команда | Описание |
|---------|----------|
| `POP` | Вставка блока M1502 на точки/вершины объектов с автонумерацией |

**Возможности команды POP:**
- Вставка блока `M1502` из `C:\LDOCS\BLOCKS\M1502.dwg`
- Поддержка объектов: POINT, LINE, LWPOLYLINE, POLYLINE, CIRCLE
- Автоматическая нумерация с опциональным префиксом/суффиксом
- Заполнение атрибутов: номер и высота (Z-координата)
- Автоматическое масштабирование по CANNOSCALEVALUE
- Режимы для окружностей: все точки / центр / квадранты
- Мультиселект и опция Previous для повторного выбора
- Автозагрузка блока при отсутствии в чертеже

**Глобальные переменные:**
- `*POP-LAST-NUM*` - последний использованный номер
- `*POP-CIRCLE-MODE*` - режим обработки окружностей (1/2/3)

---

### 🏷️ AT_ - Attribute Tools (Работа с атрибутами)

**`AT_Attribute_Tools.lsp`** - Управление атрибутами блоков

| Команда | Описание |
|---------|----------|
| `SetAttFromText` | Копирование значения из TEXT/MTEXT в атрибут HEIGHT блока |
| `AttToZMulti` | Установка Z-координаты блоков из атрибута HEIGHT |

**Особенности:**
- Поддержка десятичной запятой и точки
- Пакетная обработка множества блоков
- Автоматическое обновление координат

---

### 📍 PT_ - Point Tools (Работа с точками)

#### **`PT_Point_Tools.lsp`** - Настройка отображения и преобразование

| Команда | Описание |
|---------|----------|
| `PTT` | Установка стиля точек (квадрат+круг+крест, размер 0.3) |
| `PTTT` | Установка стиля точек с выбором размера |
| `TXT2PT` | Преобразование текста с отметками в 3D точки |

**Параметры точек:**
- PDMODE: 99 (квадрат + круг + диагональный крест)
- PDSIZE: абсолютные единицы

#### **`PT_Points_Intersection.lsp`** - Точки пересечения

| Команда | Описание |
|---------|----------|
| `PTSINT` | Создание точек на пересечениях объектов |

---

### 📐 DV_ - Deviation Tools (Расчет отклонений)

**`DV_Deviation_Tools.lsp`** - Измерение отклонений и запись в атрибуты

| Команда | Описание |
|---------|----------|
| `SetDeltaDAL` | Расчет перпендикулярного отклонения от линии/полилинии/дуги/сплайна |
| `SetDeltaDAL_CIRCLE` | Расчет радиального отклонения от окружности (±) |
| `SetDelta_Point` | Расчет отклонений ΔX, ΔY от опорной точки |

**Формат записи:**
- `Δ⊥=0.00` - отклонение от линии (всегда 2 знака)
- `Δ⊥=+0.05` - снаружи окружности
- `Δ⊥=-0.03` - внутри окружности
- `ΔE=1.05` - отклонение по X
- `ΔN=0.14` - отклонение по Y

**Целевые атрибуты:**
- `DELTA` - для отклонений от линий/окружностей
- `DELTA_X`, `DELTA_Y` - для отклонений от точки

---

### 🎨 LY_ - Layer Tools (Работа со слоями)

#### **`LY_Set_All_By_Layer.lsp`** - Установка ByLayer

| Команда | Описание |
|---------|----------|
| *(команды из файла)* | Установка всех свойств в ByLayer |

#### **`LY_Xref_Layers.lsp`** - Управление XREF

| Команда | Описание |
|---------|----------|
| `CHXA` | Управление слоями внешних ссылок |

---

## Система именования файлов

Все файлы используют префиксную систему для категоризации:

| Префикс | Категория | Описание |
|---------|-----------|----------|
| `HK_` | Hotkeys | Горячие клавиши и обертки команд |
| `BL_` | Block tools | Работа с блоками |
| `AT_` | Attribute tools | Управление атрибутами |
| `PT_` | Point tools | Работа с точками |
| `DV_` | Deviation tools | Расчет отклонений |
| `LY_` | Layer tools | Работа со слоями |

---

## Установка и использование

1. Поместите все файлы в папку `c:\1\`
2. Добавьте в `acad.lsp` или `acaddoc.lsp`:
   ```lisp
   (load "c:/1/MainLoader.lsp")
   ```
3. Перезапустите AutoCAD или выполните `(load "c:/1/MainLoader.lsp")`

---

## Зависимости

- AutoCAD с поддержкой LISP
- Visual LISP (vl-load-com)
- Некоторые команды требуют VBA макросов (MM, RB, hi)
- Блок M1502 для команды POP: `C:\LDOCS\BLOCKS\M1502.dwg`

---

## Технические детали

### Порядок загрузки файлов

Файлы загружаются в следующем порядке (см. `MainLoader.lsp`):

1. HK_Hotkeys.lsp
2. BL_Block_Tools.lsp
3. BL_Delete_Duplicate_Blocks.lsp
4. BL_Select_Blocks_On_Line.lsp
5. BL_Insert_Block_To_Points.lsp
6. AT_Attribute_Tools.lsp
7. PT_Point_Tools.lsp
8. PT_Points_Intersection.lsp
9. DV_Deviation_Tools.lsp
10. LY_Set_All_By_Layer.lsp
11. LY_Xref_Layers.lsp

### Глобальные переменные

- `*main-lsp-dir*` - путь к директории со скриптами
- `*POP-LAST-NUM*` - последний номер для команды POP
- `*POP-CIRCLE-MODE*` - режим обработки окружностей (1=все точки, 2=центр, 3=квадранты)

---

## История изменений

### Версия 2.0 (Январь 2026)
- ✅ Реорганизация структуры файлов с префиксной системой
- ✅ Разделение большого файла (600+ строк) на логические модули
- ✅ Переименование команды INSBLP → POP
- ✅ Добавлена поддержка PREFIX/SUFFIX для нумерации
- ✅ Улучшена обработка отмены команд (Escape)
- ✅ Удалены неиспользуемые скрипты:
  - 01_Language_Switch.lsp
  - 02_Block_Attributes.lsp
  - 05_Deviation_Arrows.lsp
  - 11_Points_On_Polyline.lsp
  - LY_Colors_By_Layer.lsp

### Версия 1.0
- Базовая функциональность
- Нумерованная система файлов (01-12)

---

## Автор и поддержка

Для вопросов и предложений обращайтесь к разработчику.

**Рабочая директория:** `c:\1\`  
**Формат:** AutoCAD LISP (LSP)
